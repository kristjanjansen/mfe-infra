name: PR Preview Environment

on:
  workflow_call:
    inputs:
      service_name:
        required: true
        type: string
      port:
        required: true
        type: string
    secrets:
      infra_token:
        required: false

jobs:
  preview:
    runs-on: self-hosted
    permissions:
      pull-requests: write
      issues: write
      contents: read

    env:
      SERVICE_NAME: ${{ inputs.service_name }}
      PORT: ${{ inputs.port }}
      PR: ${{ github.event.number || 1 }}
      INFRA_TOKEN: ${{ secrets.infra_token }}

    steps:
      - uses: actions/checkout@v4

      - name: Infra token present?
        shell: bash
        run: |
          if [ -n "${INFRA_TOKEN}" ]; then
            echo "infra_token_present=true"
          else
            echo "infra_token_present=false"
          fi

      - name: Checkout mf-infra (for provider config)
        uses: actions/checkout@v4
        with:
          repository: kristjanjansen/mf-infra
          path: mf-infra
          token: ${{ github.token }}

      - name: Load provider config
        shell: bash
        run: |
          set -euo pipefail
          PROVIDER="${PROVIDER:-${{ vars.PROVIDER || 'local' }}}"
          source "mf-infra/k8s/providers/${PROVIDER}/config.env"
          echo "DOMAIN=${DOMAIN}" >> "$GITHUB_ENV"
          echo "PROTOCOL=${PROTOCOL}" >> "$GITHUB_ENV"
          echo "REGISTRY=${REGISTRY}" >> "$GITHUB_ENV"

      - name: Compute preview identifiers
        shell: bash
        run: |
          set -euo pipefail

          PR_NUMBER='${{ github.event.number || 1 }}'
          TITLE='${{ github.event.pull_request.title || '' }}'

          SUFFIX="pr-${PR_NUMBER}"
          EVENT_ENVIRONMENT="pr"

          shopt -s nocasematch
          if [[ "$TITLE" =~ ^RELEASE[[:space:]]+([^[:space:]]+).*$ ]]; then
            RAW_VERSION="${BASH_REMATCH[1]}"

            SAFE_VERSION="$(echo "$RAW_VERSION" | tr -c 'a-zA-Z0-9' '-' | tr -s '-' | sed 's/^-*//;s/-*$//')"
            if [ -z "$SAFE_VERSION" ]; then
              SAFE_VERSION="0-0-0"
            fi

            SUFFIX="rel-${SAFE_VERSION}"
            EVENT_ENVIRONMENT="rel"
          fi
          shopt -u nocasematch

          if [ -n "${REGISTRY}" ]; then
            FULL_IMAGE="${REGISTRY}/${SERVICE_NAME}:${SUFFIX}"
          else
            FULL_IMAGE="${SERVICE_NAME}:${SUFFIX}"
          fi

          echo "NAMESPACE=${SERVICE_NAME}-${SUFFIX}" >> "$GITHUB_ENV"
          echo "IMAGE=${FULL_IMAGE}" >> "$GITHUB_ENV"
          echo "HOST=${SERVICE_NAME}-${SUFFIX}.${DOMAIN}" >> "$GITHUB_ENV"
          echo "DEPLOY_URL=${PROTOCOL}://${SERVICE_NAME}-${SUFFIX}.${DOMAIN}" >> "$GITHUB_ENV"
          echo "EVENT_ENVIRONMENT=${EVENT_ENVIRONMENT}" >> "$GITHUB_ENV"

      - name: Log in to container registry
        if: ${{ env.REGISTRY != '' }}
        run: echo "${{ github.token }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and push Docker image
        run: |
          docker build \
            --build-arg SERVICE_NAME="$SERVICE_NAME" \
            -t "$IMAGE" .
          if [ -n "${REGISTRY}" ]; then
            docker push "$IMAGE"
          fi

      - name: Deploy preview via infra action
        uses: kristjanjansen/mf-infra/.github/actions/deploy-preview@main
        with:
          service_name: ${{ env.SERVICE_NAME }}
          namespace: ${{ env.NAMESPACE }}
          image: ${{ env.IMAGE }}
          host: ${{ env.HOST }}
          port: ${{ env.PORT }}

      - name: Comment PR
        uses: kristjanjansen/mf-infra/.github/actions/comment-preview@main
        with:
          header: Preview - ${{ env.SERVICE_NAME }}
          message: |
            🚀 Preview ready: ${{ env.DEPLOY_URL }}

      - name: Record deploy event
        if: ${{ env.INFRA_TOKEN != '' }}
        uses: kristjanjansen/mf-infra/.github/actions/record-deploy-event@main
        with:
          token: ${{ env.INFRA_TOKEN }}
          app_name: ${{ env.SERVICE_NAME }}
          environment: ${{ env.EVENT_ENVIRONMENT }}
          deploy_url: ${{ env.DEPLOY_URL }}
          status: ${{ job.status }}

  cleanup:
    if: github.event.action == 'closed'
    runs-on: self-hosted

    env:
      SERVICE_NAME: ${{ inputs.service_name }}

    steps:
      - name: Compute preview identifiers
        shell: bash
        run: |
          set -euo pipefail

          PR_NUMBER='${{ github.event.number || 1 }}'
          TITLE='${{ github.event.pull_request.title || '' }}'

          SUFFIX="pr-${PR_NUMBER}"
          SHOULD_DELETE="true"

          shopt -s nocasematch
          if [[ "$TITLE" =~ ^RELEASE[[:space:]]+([^[:space:]]+).*$ ]]; then
            RAW_VERSION="${BASH_REMATCH[1]}"

            SAFE_VERSION="$(echo "$RAW_VERSION" | tr -c 'a-zA-Z0-9' '-' | tr -s '-' | sed 's/^-*//;s/-*$//')"
            if [ -z "$SAFE_VERSION" ]; then
              SAFE_VERSION="0-0-0"
            fi

            SUFFIX="rel-${SAFE_VERSION}"
            SHOULD_DELETE="false"
          fi
          shopt -u nocasematch

          echo "NAMESPACE=${SERVICE_NAME}-${SUFFIX}" >> "$GITHUB_ENV"
          echo "SHOULD_DELETE=${SHOULD_DELETE}" >> "$GITHUB_ENV"

      - uses: kristjanjansen/mf-infra/.github/actions/delete-preview@main
        if: env.SHOULD_DELETE == 'true'
        with:
          namespace: ${{ env.NAMESPACE }}
