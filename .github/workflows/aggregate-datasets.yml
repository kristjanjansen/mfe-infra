name: Aggregate datasets

on:
  workflow_dispatch:
  push:
    paths:
      - "deploys/**"

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency:
      group: datasets-build
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync main
        shell: bash
        run: git pull --rebase origin main

      - name: Debounce
        shell: bash
        run: sleep 5

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build datasets
        shell: bash
        run: node .github/workflows/aggregate-datasets.mjs

      - name: Prepare Pages artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf site
          mkdir -p site/state
          cp index.html site/index.html
          cp index.js site/index.js
          cp index.css site/index.css
          cp state/deploys.json site/state/deploys.json
          cp state/graph.json site/state/graph.json

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Commit datasets
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add state/deploys.json state/graph.json || true

          if git diff --cached --quiet; then
            exit 0
          fi

          git commit -m "chore: update state [skip agg]"

          for i in 1 2 3 4 5; do
            if git push; then
              exit 0
            fi
            git pull --rebase --autostash origin main || true
            sleep $((i * 2))
          done

          echo "Failed to push datasets after retries" >&2
          exit 1

  deploy_pages:
    runs-on: ubuntu-latest
    needs: build
    concurrency:
      group: datasets-deploy
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
